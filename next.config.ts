/** @type {import('next').NextConfig} */
const nextConfig = {
  // eslint: { ignoreDuringBuilds: true }, // Deprecated in newer versions, moved to 'next lint' flags or ignored
  // Fix for specific packages if needed
}

module.exports = nextConfig
